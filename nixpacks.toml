[phases.setup]
nixPkgs = ["nodejs_20", "python311", "python311Packages.pip"]

[phases.install]
cmds = [
  "npm install",
  "pip3 install telethon qrcode pillow --break-system-packages"
]

[start]
cmd = "node server.js"
